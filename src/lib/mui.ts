import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FC6D33",
    },
    secondary: {
      main: "#FFDBCC",
    },
  },
});

export default theme;
