import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#585D27",
    },
    secondary: {
      main: "#775B45",
    },
  },
});

export default theme;
